# src/config/__init__.py

"""
Configuration package.

This package is responsible for managing the application's environment-specific
settings. It ensures that all configuration variables are loaded, validated,
and made available from a single, consistent source.

Modules:
    - environment: Defines the application's configuration model and loads
                   settings from environment variables and .env files.
"""
